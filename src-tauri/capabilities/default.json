{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the ShellWarden main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
